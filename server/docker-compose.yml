services:
  mongo:
    image: mongo:8
    container_name: roomcall-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - roomcall-mongo:/data/db

volumes:
  roomcall-mongo:
